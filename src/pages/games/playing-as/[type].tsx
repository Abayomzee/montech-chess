import React from "react";
import Playing from "components/pages/Playing";

interface Props {}
const UserType: React.FC<Props> = () => {
  // Data to render
  return (
    <Playing />
  );
};

export default UserType;
